<h1>Welcome to My Blog</h1>
<p>
  <!--  This is the embeded ruby code syntax (less than and percentage signs)-->
  <!-- This group of symbols (less than, percentage, and equal signs) act like a puts message -->
  <% @posts.each do |post| %>
    <article>
      <h2>
        <a href="show/<%= post[:id] %>"><%= post[:title] %></a>
      </h2>
      <h3> By: <%= post[:author] %> </h3>
      <p> <%= post[:body] %> </p>
    </article>
  <% end %>
</p>
