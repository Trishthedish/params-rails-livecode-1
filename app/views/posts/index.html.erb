<h1>Welcome to My Blog</h1>
<p>
  <!--  This is the embeded ruby code syntax (less than and percentage signs aka(cone))-->
  <!-- This group of symbols (less than, percentage, and equal signs) act like a puts message -->
  <% @posts.each do |post| %>
    <article>
      <h2>
        <%= link_to(post.title, show_path(post[:id]) ) %>
        <!-- <a href="show/(cone) post[:id] (cone)">(cone=) post[:title] (cone)</a> -->
      </h2>
      <h3> By: <%= post.author %> </h3>
      <p> <%= post.body %> </p>
    </article>
  <% end %>
</p>
